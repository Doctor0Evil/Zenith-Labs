# .github/workflows/bootstrap-docs.yml
name: Ensure API Reference Exists

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  ensure-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create docs/api-reference.md if missing
        shell: pwsh
        run: |
          $docPath = "docs/api-reference.md"
          if (-not (Test-Path $docPath)) {
            New-Item -ItemType Directory -Force -Path (Split-Path $docPath) | Out-Null
            @"
# ALN Programming Language – API Reference

_Last updated: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')_  
_Compliance hash: $(git rev-parse HEAD)_

## Overview
This document provides a complete, audit-ready reference for all ALN public APIs.

## Authentication
<!-- Auth details here -->

## Endpoints
<!-- Endpoint list -->

_Automatically generated by ensure-docs job_
"@ | Set-Content -Path $docPath -Encoding UTF8
          }

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/api-reference.md
          if ! git diff --cached --quiet; then
            git commit -m "chore(docs): ensure api-reference.md present [ci skip]"
            git push
          fi
